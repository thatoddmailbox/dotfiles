alias c='cd'
alias m='mkdir -p'
alias s='ssh'

school() {
	cd ~/Documents/School/2022FA
	if [[ $1 ]]; then
		cd *$1*
	fi
}
gh() {
	cd ~/Documents/GitHub
	if [[ $1 ]]; then
		cd *$1*
	fi
}
gos() {
	cd ~/go/src/github.com
	if [[ $1 ]]; then
		cd *$1*
	fi
}
gost() {
	cd ~/go/src/github.com/thatoddmailbox
	if [[ $1 ]]; then
		cd *$1*
	fi
}
gosm() {
	cd ~/go/src/github.com/MyHomeworkSpace
	if [[ $1 ]]; then
		cd *$1*
	fi
}
gosn() {
	cd ~/go/src/github.com/NoteToScreen
	if [[ $1 ]]; then
		cd *$1*
	fi
}
